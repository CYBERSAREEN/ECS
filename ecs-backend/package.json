{
  "name": "ecs-backend",
  "version": "1.1.0",
  "description": "Excelon Cyber Solutions — Express API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "setup:hash": "node -e \"const bcrypt=require('bcryptjs'); if(!process.env.ADMIN_PASSWORD){console.error('Set ADMIN_PASSWORD env var first');process.exit(1);} bcrypt.hash(process.env.ADMIN_PASSWORD,12).then(h=>console.log(h));\"",
    "build": "echo 'No build step required'"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "bcryptjs": "^2.4.3",
    "compression": "^1.8.1",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "ejs": "^3.1.10",
    "express": "^4.19.2",
    "express-rate-limit": "^7.3.1",
    "express-validator": "^7.1.0",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "resend": "^3.5.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.3"
  }
}
