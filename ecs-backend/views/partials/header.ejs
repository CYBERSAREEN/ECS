<%
  var nav = [
    { href: "/", label: "Home" },
    { href: "/services", label: "Services" },
    { href: "/projects", label: "Projects" },
    { href: "/about", label: "About" },
    { href: "/ideas", label: "Ideas" },
    { href: "/contact", label: "Contact" }
  ];
  var current = typeof active !== 'undefined' ? active : '';
%>
<nav class="navbar" id="navbar">
  <div class="container">
    <a href="/" class="nav-logo">
      <svg width="24" height="31" viewBox="0 0 52 68" fill="none" aria-hidden="true">
        <path d="M7 0H45C48.866 0 52 3.134 52 7V45L26 68L0 45V7C0 3.134 3.134 0 7 0Z" style="fill:var(--red);"/>
        <text x="26" y="30" text-anchor="middle" font-family="Arial,sans-serif" font-weight="700" font-size="15" style="fill:var(--cream);" letter-spacing="1.5">ECS</text>
        <path d="M17 53L26 68L35 53Z" style="fill:var(--charcoal);" opacity="0.5"/>
      </svg>
      <span class="nav-logo__wm"><span class="wm-top">Excelon Cyber</span><span class="wm-sub">Solutions Pvt. Ltd.</span></span>
    </a>
    <div class="nav-links">
      <% nav.forEach(function(item){ %>
        <a href="<%= item.href %>" class="<%= current === item.href ? 'active' : '' %>"><%= item.label %></a>
      <% }); %>
    </div>
    <div class="nav-cta">
      <a href="/contact" class="btn btn-primary" style="padding:10px 18px;font-size:.85rem;">Book a Service
        <svg class="arrow" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M5 12h14M13 6l6 6-6 6"/></svg>
      </a>
    </div>
    <button class="nav-toggle" id="navToggle" aria-label="Menu">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M3 6h18M3 12h18M3 18h18"/></svg>
    </button>
  </div>
</nav>
<div class="mobile-menu" id="mobileMenu">
  <% nav.forEach(function(item){ %>
    <a href="<%= item.href %>" class="<%= current === item.href ? 'active' : '' %>"><%= item.label %></a>
  <% }); %>
  <a href="/contact" class="btn btn-primary">Book a Service</a>
</div>
